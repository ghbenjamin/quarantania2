#include <components/DescriptionComponent.h>

DescriptionComponent::DescriptionComponent(std::string const& title, std::string const& sd, std::string const& ld)
: title(title), shortDescription(sd), longDescription(ld) {}

